# 01 INVENTORY

Run the package's inventory step. Record which prerequisites are `FOUND` and
which are `MISSING`; do not turn an inventory result into a readiness claim.
Use the canonical package file `01_INVENTORY.md` for exact commands.
