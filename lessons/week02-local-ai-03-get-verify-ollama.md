# 03 GET/VERIFY OLLAMA

Use the canonical package file `03_GET_OLLAMA.md` to acquire or verify the
local Ollama service. Keep the approved loopback-only setup and preserve the
first diagnostic if the machine is not ready.
