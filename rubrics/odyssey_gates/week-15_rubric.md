# Odyssey Gate — Week 15 rubric: no mandatory gate (buffer week)

Authored 2026-08-06 (course_foundry prompt 026) to close the one gap in
this repo's gate-file/rubric-file pairing: `assignments/odyssey_gates/
week-15.md` already states, explicitly, "No mandatory gate this week" —
this file exists so every gate has a paired rubric file on disk (matching
the convention every other week uses), not to invent a graded instrument
where Jeremy's already-accepted week framing says there isn't one.

## No numeric rubric this week

Week 15 (Thanksgiving; Jeremy traveling — see `planning/week-15.md`) is
async and light by design. It is not one of `docs/grading-model.md`'s
weekly-reinforcement weeks (2-5, 7-8, 10-13) or checkpoint weeks (6, 9,
14, 16) — no row in the grading model allocates points to it, and none
should be invented here.

- The optional text-analysis / external-dataset side-quests described in
  `assignments/odyssey_gates/week-15.md` are genuinely optional and are
  **not Marker-graded**, per that file's own text.
- The week's actual required, graded work is
  `assignments/A6-professional-pathway-artifacts.md`'s mid-semester
  update, which is graded under its own assignment/rubric, not this one.
- A World Bible line is optional this week; if a student did a side-quest,
  logging it is good practice but is not scored.

## Grading notes

- Do not attach `points_possible` to this gate object — leaving it
  ungraded (`submission_types: ["not_graded"]`) is the correct, intended
  mechanics for this week, not a gap to fill.
